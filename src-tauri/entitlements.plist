<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow JIT compilation (required for WebView) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Allow unsigned executable memory (required for WebView) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Disable library validation (required for plugins) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Network client access (required for IMAP/SMTP) -->
  <key>com.apple.security.network.client</key>
  <true/>
  <!-- Network server access (optional, for local servers) -->
  <key>com.apple.security.network.server</key>
  <true/>
</dict>
</plist>
